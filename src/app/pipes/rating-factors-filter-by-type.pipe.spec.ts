import { RatingFactorsFilterByTypePipe } from './rating-factors-filter-by-type.pipe';

describe('RatingFactorsFilterByTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RatingFactorsFilterByTypePipe();
    expect(pipe).toBeTruthy();
  });
});
