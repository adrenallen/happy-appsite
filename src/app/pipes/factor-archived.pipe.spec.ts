import { FactorArchivedPipe } from './factor-archived.pipe';

describe('FactorArchivedPipe', () => {
  it('create an instance', () => {
    const pipe = new FactorArchivedPipe();
    expect(pipe).toBeTruthy();
  });
});
