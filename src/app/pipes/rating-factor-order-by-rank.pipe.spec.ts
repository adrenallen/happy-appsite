import { RatingFactorOrderByRankPipe } from './rating-factor-order-by-rank.pipe';

describe('RatingFactorOrderByRankPipe', () => {
  it('create an instance', () => {
    const pipe = new RatingFactorOrderByRankPipe();
    expect(pipe).toBeTruthy();
  });
});
