import { FactorAspectArchivedPipe } from './factor-aspect-archived.pipe';

describe('FactorAspectArchivedPipe', () => {
  it('create an instance', () => {
    const pipe = new FactorAspectArchivedPipe();
    expect(pipe).toBeTruthy();
  });
});
